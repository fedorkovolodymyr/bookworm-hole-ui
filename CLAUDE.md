# bookworm-hole-ui

## Git workflow

- **Never merge/push directly to `main`.** All work lands via PR
  (`gh pr create`), reviewed and merged through GitHub, even for docs-only
  or small changes. Work on a feature branch (e.g. `block-N-<name>`), open
  a PR, merge there.
